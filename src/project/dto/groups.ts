export const create: { groups: string[] } = {
    groups: ['create'],
};

export const update: { groups: string[] } = {
    groups: ['update'],
};

export const all: { groups: string[] } = {
    groups: ['create', 'update'],
};
